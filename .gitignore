# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Credenciais e secrets
.env
.env.local
.env.*.local
.env.development
.env.production

# Regra ampla: nenhum .json entra no repo sem ser liberado explicitamente
# abaixo. Evita commitar service accounts e chaves por acidente.
*.json
!package.json
!package-lock.json
!tsconfig*.json
!metadata.json
!vercel.json
!firebase.json
!.vscode/extensions.json
!.claude/**/*.json

# Configuração pessoal do Claude Code: permissões aprovadas nesta máquina.
# O settings.json do projeto é versionado; o .local não.
.claude/settings.local.json

*service-account*.json
*credentials*.json
*keyfile*.json
firebase-applet-config.json
.gcloud/
gcloud-service-key.json

# Vercel
.vercel

# Firebase
.firebaserc
firebase-debug.log
.firebase/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
