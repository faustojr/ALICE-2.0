rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write to all collections for the test pilot environment
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
